import 'package:flutter/material.dart';

class forthinfirst extends StatelessWidget {
  const forthinfirst({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber[500],

    );
  }
}
