import 'package:flutter/material.dart';

class thirdinfirst extends StatelessWidget {
  const thirdinfirst({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber[300],

    );
  }
}
